import { Flex, Heading } from "@chakra-ui/react";

const Footer = () => {
  return (
    <Flex px={[0, 0, 10, 40]}>
      <Heading>this is the footer</Heading>
    </Flex>
  );
};

export default Footer;
